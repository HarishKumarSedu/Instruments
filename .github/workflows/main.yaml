
name: Publish Python 🐍 IVM Instruments distribution 📦 to PyPI 
on: 
  push: 
    branches: [main]
  workflow_dispatch:
    
jobs:
  build:
    name: Build distribution 📦
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.x"
    
    - name: Install pypa/build
      run: >-
        python3 -m
        pip install
        build
        --user
    - name: Build a binary wheel and a source tarball
      run: python3 -m build
    - name: Store the distribution packages
      uses: actions/upload-artifact@v3
      with:
        name: python-package-distributions
        path: dist/
    
  # publish-to-pypi:
  #   name: >-
  #     Publish Python 🐍 distribution 📦 to PyPI
  #   if: startsWith(github.ref, 'refs/tags/')  # only publish to PyPI on tag pushes
  #   needs:
  #   - build
  #   runs-on: ubuntu-latest
  #   environment:
  #     name: pypi
  #     url: https://pypi.org/project/IvmInstruments  # Replace <package-name> with your PyPI project name
  #   permissions:
  #     id-token: pypi-AgEIcHlwaS5vcmcCJDBiMmFhNGFjLTYxZTctNGZjOC1iMTNjLTA2MGI0NjQzN2VmMgACKlszLCJmOWZmZDFkZS04ZGFkLTRmYzQtOTBmOS01MmEzMzU4NGVmY2UiXQAABiCP-5sCfAtFd2Ta517m7sKWFiXlGbyyLV3fAj4N0QSA_g
  #   steps:
  #   - name: Download all the dists
  #     uses: actions/download-artifact@v3
  #     with:
  #       name: python-package-distributions
  #       path: dist/
  #   - name: Publish distribution 📦 to PyPI
  #     uses: pypa/gh-action-pypi-publish@release/v1